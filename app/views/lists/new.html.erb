<div class="container">
  <h1>Add New List</h1>

  <%= simple_form_for(@list) do |f| %>
      <%= f.input :name %>
      <%= f.input :url %>
      <%= f.submit class: "btn btn-primary" %>
  <% end %>

  <%= link_to 'Back', lists_path %>
</div>
